export interface Url {
	urlId: string;
	shortUrl: string;
	urlName: string;
	visits: number;
}

export interface Person {
	id: string;
	image: string;
	name: string;
	email: string;
	phone: string;
}
